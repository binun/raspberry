
#define RPIPORT 6666
#define SERVPORT 8888

#define NOISEFILE "noise.bin"
#define CHAIN "chain%d.bin"
#define TSTAMP "tstamp.bin"
#define CLIENTBUF 1024*100
#define MBYTE 1048576
#define ASK "ask"
#define RECENT_NOISE_TIME 2
#define FACTOR 4
#define CLEAN_FACTOR 0.7
#define SIMULATION 1
#define RPIBUFFER 10*1024
#define MIN_REPLY 512
#define MIN(X, Y) (((X) < (Y)) ? (X) : (Y))
#define MAX(X, Y) (((X) > (Y)) ? (X) : (Y))

#define MAXFILESIZE 100*MBYTE 
//1073741824
